{
  "servers": {
    "factlineage-cloud": {
      "type": "http",
      "url": "${env:FACTLINEAGE_CLOUD_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${env:FACTLINEAGE_CLOUD_TOKEN}"
      }
    }
  }
}